// Красные оттенки
void LightRed() {   // Светло-красный
    fill_solid(leds, NUM_LEDS, CHSV(0, 255, 180));
    FastLED.show();
}

void Red() {   // Красный
    fill_solid(leds, NUM_LEDS, CHSV(0, 255, 255));
    FastLED.show();
}

void DarkRed() {   // Темно-красный
    fill_solid(leds, NUM_LEDS, CHSV(0, 200, 150));
    FastLED.show();
}

// Зеленые оттенки
void LightGreen() {   // Светло-зеленый
    fill_solid(leds, NUM_LEDS, CHSV(85, 150, 200));
    FastLED.show();
}

void Green() {   // Зеленый
    fill_solid(leds, NUM_LEDS, CHSV(85, 255, 255));
    FastLED.show();
}

void DarkGreen() {   // Темно-зеленый
    fill_solid(leds, NUM_LEDS, CHSV(85, 200, 150));
    FastLED.show();
}

// Синие оттенки
void LightBlue() {   // Светло-синий
    fill_solid(leds, NUM_LEDS, CHSV(170, 150, 200));
    FastLED.show();
}

void Blue() {   // Синий
    fill_solid(leds, NUM_LEDS, CHSV(170, 255, 255));
    FastLED.show();
}

void DarkBlue() {   // Темно-синий
    fill_solid(leds, NUM_LEDS, CHSV(170, 200, 150));
    FastLED.show();
}

// Оранжевые оттенки
void LightOrange() {   // Светло-оранжевый
    fill_solid(leds, NUM_LEDS, CHSV(15, 255, 200));
    FastLED.show();
}

void Orange() {   // Оранжевый
    fill_solid(leds, NUM_LEDS, CHSV(15, 255, 255));
    FastLED.show();
}

void DarkOrange() {   // Темно-оранжевый
    fill_solid(leds, NUM_LEDS, CHSV(15, 200, 150));
    FastLED.show();
}

// Желтые оттенки
void LightYellow() {   // Светло-желтый
    fill_solid(leds, NUM_LEDS, CHSV(43, 128, 255));
    FastLED.show();
}

void Yellow() {   // Желтый
    fill_solid(leds, NUM_LEDS, CHSV(43, 255, 255));
    FastLED.show();
}

void DarkYellow() {   // Темно-желтый
    fill_solid(leds, NUM_LEDS, CHSV(43, 200, 150));
    FastLED.show();
}

// Фиолетовые оттенки
void LightPurple() {   // Светло-фиолетовый
    fill_solid(leds, NUM_LEDS, CHSV(213, 150, 200));
    FastLED.show();
}

void Purple() {   // Фиолетовый
    fill_solid(leds, NUM_LEDS, CHSV(213, 255, 255));
    FastLED.show();
}

void DarkPurple() {   // Темно-фиолетовый
    fill_solid(leds, NUM_LEDS, CHSV(213, 200, 150));
    FastLED.show();
}

// Циановые оттенки
void LightCyan() {   // Светло-циановый
    fill_solid(leds, NUM_LEDS, CHSV(160, 150, 200));
    FastLED.show();
}

void Cyan() {   // Циан
    fill_solid(leds, NUM_LEDS, CHSV(160, 255, 255));
    FastLED.show();
}

void DarkCyan() {   // Темно-циановый
    fill_solid(leds, NUM_LEDS, CHSV(160, 200, 150));
    FastLED.show();
}

// Другие цвета
void LightPink() {   // Светло-розовый
    fill_solid(leds, NUM_LEDS, CHSV(200, 150, 200));
    FastLED.show();
}

void Pink() {   // Розовый
    fill_solid(leds, NUM_LEDS, CHSV(200, 255, 255));
    FastLED.show();
}

void DarkPink() {   // Темно-розовый
    fill_solid(leds, NUM_LEDS, CHSV(200, 200, 150));
    FastLED.show();
}

// Бирюзовые оттенки
void LightTurquoise() {   // Светло-бирюзовый
    fill_solid(leds, NUM_LEDS, CHSV(160, 150, 200));
    FastLED.show();
}

void Turquoise() {   // Бирюзовый
    fill_solid(leds, NUM_LEDS, CHSV(160, 255, 200));
    FastLED.show();
}

void DarkTurquoise() {   // Темно-бирюзовый
    fill_solid(leds, NUM_LEDS, CHSV(160, 200, 150));
    FastLED.show();
}

// Серые оттенки
void LightGrey() {   // Светло-серый
    fill_solid(leds, NUM_LEDS, CHSV(0, 0, 200));
    FastLED.show();
}

void Grey() {   // Серый
    fill_solid(leds, NUM_LEDS, CHSV(0, 0, 128));
    FastLED.show();
}

void DarkGrey() {   // Темно-серый
    fill_solid(leds, NUM_LEDS, CHSV(0, 0, 100));
    FastLED.show();
}

// Коричневые оттенки
void LightBrown() {   // Светло-коричневый
    fill_solid(leds, NUM_LEDS, CHSV(30, 100, 150));
    FastLED.show();
}

void Brown() {   // Коричневый
    fill_solid(leds, NUM_LEDS, CHSV(30, 150, 100));
    FastLED.show();
}

void DarkBrown() {   // Темно-коричневый
    fill_solid(leds, NUM_LEDS, CHSV(30, 200, 50));
    FastLED.show();
}

// Золотые оттенки
void Golden() {   // Золотой
    fill_solid(leds, NUM_LEDS, CHSV(45, 255, 255));
    FastLED.show();
}

// Лавандовые оттенки
void LightLavender() {   // Светло-лавандовый
    fill_solid(leds, NUM_LEDS, CHSV(210, 150, 200));
    FastLED.show();
}

void Lavender() {   // Лавандовый
    fill_solid(leds, NUM_LEDS, CHSV(210, 255, 200));
    FastLED.show();
}

void DarkLavender() {   // Темно-лавандовый
    fill_solid(leds, NUM_LEDS, CHSV(210, 200, 100));
    FastLED.show();
}

// Персиковые оттенки
void LightPeach() {   // Светло-персиковый
    fill_solid(leds, NUM_LEDS, CHSV(15, 150, 200));
    FastLED.show();
}

void Peach() {   // Персиковый
    fill_solid(leds, NUM_LEDS, CHSV(15, 100, 200));
    FastLED.show();
}

// Изумрудные оттенки
void LightEmerald() {   // Светло-изумрудный
    fill_solid(leds, NUM_LEDS, CHSV(150, 150, 200));
    FastLED.show();
}

void Emerald() {   // Изумрудный
    fill_solid(leds, NUM_LEDS, CHSV(150, 255, 255));
    FastLED.show();
}

// Небесно-голубые оттенки
void LightSkyBlue() {   // Светло-небесно-голубой
    fill_solid(leds, NUM_LEDS, CHSV(160, 150, 200));
    FastLED.show();
}

void SkyBlue() {   // Небесно-голубой
    fill_solid(leds, NUM_LEDS, CHSV(160, 255, 255));
    FastLED.show();
}
//////////////////////////////////// Другие
void Salatovy() { //Салатовый

    fill_solid(leds, NUM_LEDS, CHSV(85, 255, 180));
    FastLED.show();

}
void Citric() { //Лимонный

    fill_solid(leds, NUM_LEDS, CHSV(60, 255, 255));
    FastLED.show();

}
void Mint() { //Мятный

    fill_solid(leds, NUM_LEDS, CHSV(150, 255, 200));
    FastLED.show();

}
void LightSilver() { //Светло-серебристый

    fill_solid(leds, NUM_LEDS, CHSV(0, 0, 180));
    FastLED.show();

}
void Magenta() { //Пурпурный

    fill_solid(leds, NUM_LEDS, CHSV(240, 255, 150));
    FastLED.show();

}
void LightBlueGreen() { //Светло-синий-зеленый

    fill_solid(leds, NUM_LEDS, CHSV(180, 255, 200));
    FastLED.show();

}
void LightOlive() { //Светло-оливковый

    fill_solid(leds, NUM_LEDS, CHSV(60, 100, 180));
    FastLED.show();

}
void SoftPink() { //Нежно-розовый

    fill_solid(leds, NUM_LEDS, CHSV(340, 255, 200));
    FastLED.show();

}
void LightPurpleBlue() { //Светло-фиолетово-синий

    fill_solid(leds, NUM_LEDS, CHSV(240, 200, 200));
    FastLED.show();

}
void LightPinkPeach() { //Светло-розовато-персиковый

    fill_solid(leds, NUM_LEDS, CHSV(15, 150, 200));
    FastLED.show();

}
void Maroon() { //Темно-бордовый

    fill_solid(leds, NUM_LEDS, CHSV(0, 255, 100));
    FastLED.show();

}
void Shortbread() { //Песочный

    fill_solid(leds, NUM_LEDS, CHSV(30, 50, 200));
    FastLED.show();

}
